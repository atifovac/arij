version: '2'
services:
    arij-mongodb:
        image: mongo:3.4.10
        ports:
            - "27017:27017"
        command: mongos --configdb arij-mongodb-config
    arij-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    arij-mongodb-config:
        image: mongo:3.4.10
        container_name: arij-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
